{% extends 'backoffice/base_admin.html.twig' %}

{% block title %}Connexion{% endblock %}

{% block main_page %}
    <div class="container-fluid page-body-wrapper full-page-wrapper">
        <div class="content-wrapper login-container d-flex align-items-center auth px-0">
            <div class="row w-100 mx-0">
                <div class="col-lg-3 mx-auto">
                    <div class="auth-form-light text-left py-3 px-4 px-sm-3">
                        <div class="brand-logo text-center">
                            <img src="{{ asset('images/logo/Logo-full-dark.png') }}" class="text-center"/>
                        </div>
                        {% if app.user %}
                            <div class="mb-3">
                                You are logged in as {{ app.user.userIdentifier }}, <a href="{{ path('app_logout') }}">Logout</a>
                            </div>
                        {% endif %}
                        <form class="pt-3" action="{{ path('reset_password_submit') }}" method="post">
                            <div>
                                <input class="mb-3 form-control form-control-lg" placeholder="Nom d'utilisateur" type="text" id="username" name="username" required>
                            </div>
                            <div>
                                <input class="mb-3 form-control form-control-lg" type="password" placeholder="Mots de passe"  id="new_password" name="new_password" required>
                            </div>
                            <div>
                                <input class="mb-3 form-control form-control-lg" type="password" placeholder="Confirmer le mots de passe" id="confirm_password" name="confirm_password" required>
                            </div>
                            <div>
                                <button class="mb-3 btn btn-block login-btn btn-primary btn-sm w-100 font-weight-medium auth-form-btn" type="submit">Reset Password</button>
                            </div>
                        </form>
                        <a href="{{ path('app_login') }}" class=" justify-content-between align-items-center auth-link text-black">Se connecter</a>
                    </div>
                </div>
            </div>
        </div>
        <!-- content-wrapper ends -->
    </div>
{% endblock %}